import * as React from 'react'
import Search from './Search'

/**
 * renders the application
 * @return {JSX.Element}
 */
const App = (): JSX.Element => (
  <div>
    <Search />
  </div>
)

export default App
